#!/bin/sh
# 2011-07-05 12:21:43
# Backup file and recover

if [ -e $2 ] ; then
  case $1 in
    -m) mv $2 "$2".bak ;;
    -t) tar zcfp /home/backup/"$2"-`date +%Y-%m-%d` $2 ;;
    -r) mv $2 $(echo $2 | sed 's/.bak//') ;;
  esac
fi
